import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return (
    <div className="flex justify-center items-center w-screen h-screen">
      <Button variant={"destructive"}>test me</Button>
    </div>
  );
};

export default page;
